<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Hardened-runtime entitlements for a signed/notarized build. The mic and
       ScreenCaptureKit (system audio) both need the audio-input entitlement;
       the usage-description strings live in Info.plist. -->
  <key>com.apple.security.device.audio-input</key>
  <true/>
  <key>com.apple.security.device.microphone</key>
  <true/>
  <!-- EventKit reads the Mac's calendars for the "Coming up" list. Without this
       the hardened runtime refuses the read and reports the permission as denied
       without ever showing a prompt. -->
  <key>com.apple.security.personal-information.calendars</key>
  <true/>
</dict>
</plist>
